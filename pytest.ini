# content of pytest.ini
[pytest]
console_output_style = classic
python_files = test_*.py
# Prepend the repo root to sys.path so `import bote` always resolves to the
# local source, regardless of how pytest is invoked (the `pytest` console entry
# point does not add the cwd) and even if an older bote is installed.
pythonpath = .
